# Changelog

## [0.6.0] - 2026-08-10

### 新增

- **运动最低目标设定** — 运动页面家长模式可设置每日最低太阳数（默认 3 个），API `GET/PUT /api/exercise-stats/min-suns`
- **每日总结每 6 小时推送** — 推送时刻改为 7:00 / 13:00 / 19:00（原 21:00 一次），去重键按日期+小时
- **运动未达标提醒** — 19:00 后若今日太阳数未达目标，每 30 分钟推送运动提醒
- **每日总结强化运动统计** — 显示"今日太阳 ☀️ X / 目标 Y"，未达标提示差多少
- **合并统计页到周报页** — 删除统计页，周报页新增累计概览、月历打卡、近7天折线

### 修复

- 修复 `computeDueReminders` 死循环导致服务无响应（前端卡"加载中"）
- 修复备份导入路径不一致导致恢复失败
- 修复周报测试对运行日的敏感（周一失败）
- 修复备份路由测试 mock 缺少 `BACKUP_DIR`

### 优化

- dev:server 移除 `--watch`，避免 Windows 下偶发重启导致接口 `ECONNREFUSED`
- 版本号提升至 0.6.0

## [0.5.0] - 2026-08-10

### 重要修复：服务启动后无响应（前端卡"加载中"）

**根因**：`computeDueReminders`（`src/server/schedule-reminders.ts`）中周期性课程的扫描 `for` 循环以 `found < lookForwardOcc` 为终止条件，但 `found` 仅在 `matchesDate` 为 true 且课程未结束时递增。当课程带 `endDate`（有限日期范围内匹配次数达不到 60 次）或 `repeatDays` 为空时，`d` 会无限 `setDate(+1)` 向后扫描直到 Date 溢出，导致**死循环、CPU 100%、服务无响应**，每分钟一次的 `pushClassReminders` 定时器首次触发即卡死。

**影响**：导入含带 `endDate` 的 weekly 课程或含周日（0）repeatDays 的旧备份后，后端首次定时器触发即死循环，前端所有请求超时，页面一直显示"加载中"，无法使用。

**修复**：给周期性扫描增加硬性上限 `scanEnd = now + lookForwardOcc * 7` 天，超过即停止扫描，既保留"找够未来次数"的语义，又杜绝无限扫描。

**验证**：新增 2 个回归测试（带 endDate 的 weekly / 空 repeatDays），修复前死循环崩溃、修复后通过；服务实测 132 秒跨多个定时器周期稳定，CPU 正常。

### 其他修复

- 备份导入路径不一致导致恢复失败（`routes/backup.ts` 用 `__dirname` 相对路径，与 `backup.ts` 的 `BACKUP_DIR` 不一致，统一改用 `BACKUP_DIR`）
- dev 模式后端与前端文件监视共存时启动异常（`dev:server` 改用 `node --watch`）

### 仓库

- 修复 README 误导性的"零配置"描述，补充 `.env` 创建说明

## [0.4.0] - 2026-08-09

### 新增

- **学习任务钉钉推送** — 每天 07:00 推送今日学习任务、21:00 推送今日完成总结、每周日 22:00 推送本周总结，复用课程推送的 webhook 与熔断保护
- **学习任务 Markdown** — 任务清单、日总结（完成数/完成率/积分）、周总结（打卡天数/积分/分科目分布），均含钉钉关键词
- **学习任务手动推送按钮** — 设置页可手动触发今日任务、今日总结、本周总结推送
- **课程表钉钉推送** — 每日课表 + 未来7天总览 + 上课前1小时提醒，支持手动推送
- **上课 App 字段** — 远程课可填写腾讯会议/钉钉课堂等，推送消息中自动标注
- **7天总览** — 每天首次推送包含未来7天课程总览，按日期分组、标注星期
- **推送配置** — 设置页新增钉钉群机器人 webhook 配置、开关、测试推送
- **手动推送按钮** — 设置页可手动推送今日课表和7天总览，方便测试和修改课程后补发

### 修复

- 推送配置保存失败导致重启后配置丢失（哨兵用户外键约束问题）
- 设置页家长模式无法开启（缺少密码验证开启逻辑）

### 测试

- 全量 162 个测试通过
- 新增 notifier 格式断言、schedule-push API 端点测试

## [0.3.0] - 2026-08-08

### 新增

- **课程表** — 月历视图管理学校课程和补习班，支持每周重复/单次课程
- **自动提醒** — 提前1天浏览器通知 + 提前30分钟站内横幅提醒
- **双模式部署** — 同一套代码支持单机（SQLite）和网络（PostgreSQL + JWT）部署
- **部署文档** — 5 种部署方案（Railway / Render / Zeet / Docker / PM2 自托管）

### 修复

- 补打卡积分未刷新（MakeupPage 缺少 refreshData 调用）
- 运动打卡页面白屏（fetchExerciseOverview 缺少 API 调用）
- 测试环境崩溃（vitest 4.1.10 + std-env@4.2.0 ESM 兼容问题）

### 测试

- 新增 19 个测试用例（课程表 CRUD 集成 + 提醒算法纯函数）
- 全量 134 个测试用例通过

### 文档

- README 更新：功能特性、技术栈、项目结构、课程表说明
- 新增 `docs/deploy-network.md`（Railway 部署手册）
- 新增 `docs/deploy-network-free.md`（Fly.io + Neon 免费部署手册）
- 新增 4 份 ADR（架构决策记录）